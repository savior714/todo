node_modules

# Python (로컬 스크립트 실행 시)
__pycache__/
*.py[cod]

.next
storybook-static
.env
.env.*
!.env.example
.env.vercel.dev
.env.vercel.pull
.vercel
.next-build-turso.sqlite

# WIP patches (로컬만)
.git-snapshots/

# TypeScript incremental
tsconfig.tsbuildinfo

# 일회성 어드민 마이그레이션 시크릿 (절대 커밋 금지)
.migrate-secret.local

# bun이 SSOT이면 npm lock은 추적하지 않음
package-lock.json
.env*.local

# 로컬 전용 ai_worklog 심볼릭 링크(타 머신 경로) — 추적 금지
tools/ai_worklog

# Playwright MCP 실행 산출물
.playwright-mcp/

# 아카이브된 플랜 (git 관리 대상 아님)
docs/plans/archive/
